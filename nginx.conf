server {
    listen 80;
    server_name vivekbasal.co.uk www.vivekbasal.co.uk;

    root /usr/share/nginx/html;
    index index.html;

    location / {
    }

    gzip on;
    gzip_types text/html text/css application/javascript;
}